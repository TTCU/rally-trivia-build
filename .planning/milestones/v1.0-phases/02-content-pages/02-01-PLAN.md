---
phase: 02-content-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-05

must_haves:
  truths:
    - "Landing page opens with a full-viewport dark hero containing an outcome-focused headline, subheadline, and a visible 'Request a Demo' CTA button"
    - "Below the hero, a value propositions section explains why Rally Trivia with icon cards in a responsive grid"
    - "A dual-audience section shows side-by-side cards for 'For Nonprofits' and 'For Corporate Events' with tailored messaging and CTAs"
    - "A 'How It Works' section presents 3-4 numbered steps in a responsive layout"
    - "A social proof section displays 3-4 impressive stat numbers on a dark background — no testimonials, no counting animations"
    - "A final CTA section at the bottom repeats the demo request call to action"
    - "All sections have animate-on-scroll class for scroll-triggered fade-in"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Complete landing page with 6 marketing sections"
      contains: "min-h-screen bg-navy"
  key_links:
    - from: "src/pages/index.astro"
      to: "/contact"
      via: "CTA button href"
      pattern: "href=.*contact"
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "layout wrapper import"
      pattern: "import BaseLayout"
---

<objective>
Build the complete landing page with all 6 marketing sections: full-viewport dark hero, value propositions, dual-audience cards, how it works steps, stats-only social proof, and final CTA.

Purpose: The landing page is the primary conversion funnel — visitors must immediately understand what Rally Trivia does and be guided toward requesting a demo.
Output: `src/pages/index.astro` fully populated with marketing content and responsive layouts.
</objective>

<execution_context>
@/Users/jslitzker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jslitzker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-pages/02-CONTEXT.md
@.planning/phases/02-content-pages/02-RESEARCH.md
@src/pages/index.astro
@src/layouts/BaseLayout.astro
@src/components/ScrollAnimations.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build landing page hero and value proposition sections</name>
  <files>src/pages/index.astro</files>
  <action>
Replace the placeholder content in `src/pages/index.astro` with the first three marketing sections:

**Section 1 — Hero (LAND-01):**
- Full-viewport dark hero: `min-h-screen bg-navy flex items-center justify-center px-4 animate-on-scroll`
- Inner container: `max-w-4xl mx-auto text-center`
- `h1` with outcome-focused headline — lead with what the audience gets (engagement, fundraising, memorable events). Warm, approachable tone. Use `text-5xl md:text-7xl font-bold text-white leading-tight mb-6`. Include a `<span class="text-gold">` accent on one key word
- Subheadline: `text-xl md:text-2xl text-white/70 max-w-2xl mx-auto mb-10` — 1-2 sentences reinforcing the value
- CTA button: `<a href="/contact">` styled `inline-block px-8 py-4 text-lg font-bold rounded-brand bg-gold text-navy hover:bg-gold/90 transition-colors duration-200` with text "Request a Demo"

**Section 2 — Value Propositions (LAND-02):**
- Light section: `py-24 px-4 bg-white animate-on-scroll`
- Inner container: `max-w-7xl mx-auto`
- Section heading: `text-4xl font-bold text-navy text-center mb-4` (e.g., "Why Rally Trivia?")
- Subtitle: `text-lg text-navy/60 text-center max-w-2xl mx-auto mb-16`
- 3-column responsive grid: `grid grid-cols-1 md:grid-cols-3 gap-8`
- Each card: icon (inline SVG, `text-electric-blue w-8 h-8` using `set:html`), bold title (`text-xl font-bold text-navy mb-2`), short description (`text-navy/60`)
- Generate 3 value props: focus on engagement/fun, fundraising results, ease of use. Keep copy scannable and punchy — short headlines, 1-2 sentence descriptions

**Section 3 — Dual Audience (LAND-03):**
- Light background: `py-24 px-4 bg-slate-50 animate-on-scroll`
- Inner container: `max-w-7xl mx-auto`
- Section heading: `text-4xl font-bold text-navy text-center mb-16`
- 2-column grid: `grid grid-cols-1 md:grid-cols-2 gap-8`
- "For Nonprofits" card: white bg (`bg-white rounded-brand p-8 shadow-sm border border-navy/10`), gold icon, `h3` heading, 4-5 bullet benefits with `text-gold` checkmarks, CTA link to /contact styled `bg-deep-blue text-white`
- "For Corporate Events" card: dark bg (`bg-navy rounded-brand p-8 shadow-sm`), gold icon, `h3` heading in `text-white`, 4-5 bullet benefits in `text-white/70`, CTA link to /contact styled `bg-gold text-navy`
- Copy emphasis: fun/engagement first, results second per user direction

Tone for all copy: warm, conversational, approachable — like talking to a trusted event planning partner. Scannable and punchy. No stock photos.
  </action>
  <verify>Run `npm run build` — no TypeScript or Astro build errors. Open in dev server (`npm run dev`) and confirm first 3 sections render with correct dark/light alternation and responsive grid layouts.</verify>
  <done>Landing page shows full-viewport dark hero with headline + CTA, a 3-column value props section, and side-by-side dual-audience cards — all responsive and using brand colors.</done>
</task>

<task type="auto">
  <name>Task 2: Add how-it-works, social proof, and final CTA sections</name>
  <files>src/pages/index.astro</files>
  <action>
Continue building `src/pages/index.astro` by adding the remaining 3 sections after the dual-audience section:

**Section 4 — How It Works (LAND-05):**
- Light section: `py-24 px-4 bg-white animate-on-scroll`
- Inner container: `max-w-7xl mx-auto`
- Section heading: `text-4xl font-bold text-navy text-center mb-16` — "How It Works"
- 3 numbered steps in a grid: `grid grid-cols-1 md:grid-cols-3 gap-12`
- Each step: large faded step number (`text-6xl font-bold text-gold/20 mb-4 select-none`), bold title (`text-xl font-bold text-navy mb-2`), description (`text-navy/60`)
- Steps describe the journey: choose/customize → setup/configure → event day success. Keep descriptions to 1-2 sentences each
- Center on mobile (`text-center items-center`), left-align on desktop (`md:items-start md:text-left`)

**Section 5 — Social Proof (LAND-04):**
- Dark section: `py-24 px-4 bg-deep-blue animate-on-scroll`
- Inner container: `max-w-5xl mx-auto text-center`
- Section heading: `text-3xl font-bold text-white mb-16`
- 4 stats in a grid: `grid grid-cols-2 md:grid-cols-4 gap-12`
- Each stat: large number (`text-5xl font-bold text-gold mb-2`), label (`text-white/60 text-sm uppercase tracking-wide`)
- Use plausible-but-impressive placeholder numbers. Suggested: "500+" events, "$2M+" raised, "10,000+" guests, "50+" cities. Add HTML comments noting these are placeholder stats for easy replacement
- NO fake testimonials, NO counting animations, NO attributed quotes — stats only per user decision

**Section 6 — Final CTA:**
- Dark section: `py-24 px-4 bg-navy animate-on-scroll`
- Inner container: `max-w-3xl mx-auto text-center`
- Heading: `text-4xl font-bold text-white mb-4` — strong closing headline
- Subtitle: `text-xl text-white/70 mb-10`
- CTA button: same style as hero CTA — `<a href="/contact">` with `bg-gold text-navy` styling, "Request a Demo" text

Ensure all 6 sections flow in the correct order: Hero → Value Props → Dual Audience → How It Works → Social Proof → Final CTA. Every section has `animate-on-scroll` class.
  </action>
  <verify>Run `npm run build` — no errors. In dev server, scroll through all 6 sections confirming: correct order, dark/light alternation (navy → white → slate-50 → white → deep-blue → navy), responsive layouts at 375px/768px/1280px, all CTA buttons link to /contact.</verify>
  <done>Landing page has all 6 sections in correct order with alternating backgrounds, responsive grids, scroll animations on every section, and all CTAs pointing to /contact.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with no errors
2. Landing page at `http://localhost:4321/` shows 6 distinct sections in correct order
3. Hero fills viewport with dark background, white headline, and gold CTA button
4. Value props show 3-column grid on desktop, single column on mobile
5. Dual audience shows side-by-side cards on desktop, stacked on mobile
6. How it works shows 3 numbered steps
7. Social proof shows stat numbers on dark background (no testimonials)
8. Final CTA section with demo request button
9. All CTA buttons link to `/contact`
10. All sections have `animate-on-scroll` class
</verification>

<success_criteria>
The landing page is a complete, conversion-oriented marketing page with 6 responsive sections, warm marketing copy, brand-consistent styling, and scroll animations — ready for user review.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-01-SUMMARY.md`
</output>
